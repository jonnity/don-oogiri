# ドン大喜利 要件定義 & ロードマップ

どんじゃんけん（平均台じゃんけん）の大喜利版を生大喜利イベントで運営するためのWebツール。

## 1. 確定事項

### ゲームルール（最終版）

3人 vs 3人。1本のレーン上をマーカー1つが移動する。開始位置は中央。

**用語**
- **進んでる側（advancing）**：現在マーカーを押し進めている回答＝現チャンピオン
- **阻止する側（defending）**：押し返そうとしている側＝挑戦者

**状態遷移**

```
[INITIAL_WRITING] ※試合開始時のみ
  両チーム同時に回答執筆
    先に書き終えた方 → 一定速度で自動前進開始（暫定リード＝暫定の「進んでる側」）
    後の方が「指名」された瞬間 → 前進ストップ（位置を凍結）
    指名された方が回答完了 → [VOTE] へ

[CHALLENGE_WRITING]
  進んでる側：新規回答は不要（今の回答のまま前進継続）
  阻止する側の次走者が執筆。**負けてから初めて執筆開始できる（事前執筆は不可）**
  マーカーは誰にも止められず自動前進を継続（このノンストップ前進こそが「焦らせる」核）
    → 阻止する側が「指名」された瞬間 → 前進ストップ
    → 回答完了 → [VOTE] へ

[VOTE] 進んでる側の回答 vs 阻止する側の新回答 で観客投票
  ├ 進んでる側が勝つ
  │   そのまま[CHALLENGE_WRITING]継続。負けた阻止側の次走者が執筆
  └ 阻止する側が勝つ（反転）
      その位置から反転（綱引き式）。この回答が新たな「進んでる側」になる
      旧・進んでる側の次走者が新たな「阻止する側」として[CHALLENGE_WRITING]へ

[MATCH_END] 前進中いつでも、マーカーが相手陣地の端に到達した瞬間に確定（投票を待たない）
```

**「同じ人と戦い続ける」問題について**：バラシのような特別なボーナス処理は設けない。阻止する側は負けるたびに必ず次の走者に交代するため、進んでる側の回答者が長く居座っても、対戦相手は毎回変わる。これで十分に「色んな人が答えられる」体験は担保される、という判断。

### ツールの位置づけ
- まず単体のWebツールとして開発。将来のDOC統合を見据え、DOCと同スタック・同設計思想で作る。
- 観客投票は観客のスマホからのWeb投票。

## 2. 仕様デフォルト（要レビュー）

以下は暫定決定。異論があれば差し替え。

| 項目 | デフォルト案 | 理由/備考 |
|---|---|---|
| 進んでる側の回答 | 固定（勝ち残り）。答え直さない | 「現チャンピオン回答 vs 新答」の構図を維持 |
| お題 | 1試合1お題固定。お題チェンジ機能は設けない | 同一お題の方が相対投票しやすい |
| 回答の扱い | 口頭発表が基本。投票画面は「チームA / チームB」の2択のみ。MCによる回答テキスト入力は任意機能 | 生大喜利のテンポを殺さない |
| 前進速度 | 設定パラメータ（例：中央→端まで90秒相当）。投票受付中は停止 | リハーサルで調整できるように |
| 投票の締め | MCの手動締め。任意で制限時間タイマー併用 | |
| 同数票 | 進んでる側の勝ち（現状維持＝前進継続） | 阻止する側は上回る必要がある、で整合的 |
| 観客参加 | QRコード（投影画面に表示）→ ルームコード入力なしでそのままjoin。匿名、1端末1票（セッションCookie/localStorageで制御） | イベント用途なので不正対策はゆるくてよい |
| チーム表現 | 赤チーム／青チーム。UI上は色で統一（名前と併記） | 観客投票ページで直感的に選べるように |
| 途中参加 | 投票フェーズ中でもjoin・投票可 | |

## 3. 画面構成

1. **メイン画面（ネイティブアプリ / Mac・Windowsデスクトップ）**
   - 1つのデスクトップアプリでMC操作卓とプロジェクター投影用画面の両方を兼ねる（画面切替 or 別ウィンドウ表示）
   - MC操作卓：試合作成、チーム名/メンバー登録、お題入力、フェーズ進行（執筆完了・指名・投票締め）、マーカー速度変更・位置の手動補正、試合リセット
   - 投影用画面：平均台とマーカーのアニメーション、チーム名・現走者、お題、投票中は「赤 vs 青」と得票バー、勝利演出。QRコード（観客投票ページへのリンク）を表示
   - 会場PCにインストールして使う想定。バックエンド（Cloudflare Workers）とはWebSocketで通信するが、アプリ自体はネイティブ配布物
2. **観客投票ページ（Web / スマホブラウザ）**
   - QRコードを読み取って直接アクセスするシンプルなサイト（アプリ化しない）
   - チームは赤・青の2色で表現。join → 赤/青の2択ボタン → 投票済み表示 → 結果。フェーズに応じて自動切替

## 4. 技術構成（案）

- **サーバ**: Hono + Cloudflare Workers。1試合 = 1 Durable Object ルーム。WebSocketで全クライアント（ネイティブアプリ・観客Web）を同期
- **状態**: DOのメモリ + storage。試合履歴を残したくなったらD1追加（初期は不要）
- **観客投票ページ**: Vite + React によるシンプルなWebアプリ（DOCの構成に寄せる）。ネイティブ化しない
- **メイン画面（ネイティブアプリ）**: Tauri（Rust + Vite/React）を第一候補とする。Mac/Windows対応、軽量、WebSocketクライアントとしてバックエンドに接続するだけなので、UI実装はWeb版の観客ページ・投影画面とロジックを共有しやすい
  - 理由：Electronより軽量・配布サイズが小さい／YutoのRust経験と相性が良い／DOC本体はWeb純正のままで、ネイティブアプリは「同じWebSocket API を叩く別クライアント」として疎に保てる
  - 代替案：Electronの方が枯れていて情報が多いので、Tauriでの詰まりが大きければ切り替え可
- **マーカー位置はサーバ権威**: 「前進開始時刻 + 速度 + 開始位置 + 方向」だけをブロードキャストし、各クライアント（ネイティブアプリ・観客ページ）が現在時刻から補間描画する。到達判定はDO側のアラーム/タイマーで行う
- **状態機械**（DO内）: §1の状態遷移図に対応する形で実装
  `setup → initial_writing → voting → challenge_writing → voting → (challenge_writing ⇄ voting の繰り返し) → finished`
  - `challenge_writing`（自動前進）中に端到達 → 即座に `finished`
  - `voting` 開始でマーカー停止（停止時点の位置を確定保存）
  - `voting`結果に応じて即座に `challenge_writing`（進んでる側継続）または反転して`challenge_writing`（阻止する側が新チャンピオンとして継続）に戻る。連勝カウントやボーナス処理は持たない

## 5. ロードマップ

### Phase 0: ルール・パラメータ確定（このドキュメント）
- 上記デフォルト案のレビュー、前進速度の初期値決め、ゲーム名決定

### Phase 1: コアエンジン
- DOルーム実装：状態機械、マーカー位置のサーバ権威計算、到達判定
- MC操作卓・投影画面は、この段階では**デバッグ用の素朴なWeb UI**でよい（ネイティブ化はPhase 3）
- **ゴール：投票を仮入力しながら1試合を最後まで回せる**

### Phase 2: 観客投票
- QR joinのみで参加できる観客投票Webページ（赤/青の2択）、1端末1票、リアルタイム集計、締め処理、同数処理
- 再接続処理（スマホのスリープ復帰対策）
- **ゴール：スマホ複数台で実投票して1試合成立**

### Phase 3: ネイティブアプリ化
- Phase 1のWeb UIをTauriでラップし、Mac/Windows向けデスクトップアプリ化
- MC操作卓とプロジェクター投影画面を1アプリ内で切替 or 別ウィンドウ表示できるようにする
- 投影画面にQRコード（観客投票ページへのリンク）を表示する機能を追加
- **ゴール：会場PC1台にインストールしたアプリだけでMC進行と投影ができる（観客はWebのまま）**

### Phase 4: 演出
- 平均台＆マーカーのアニメーション（サーバ時刻同期の補間描画）
- 投票結果のバー演出、反転（綱引き）演出、勝利演出
- DOCの「公共施設×サイバー」トンマナに寄せるかはここで判断

### Phase 5: 運用機能
- 前進速度のライブ調整、マーカー位置の手動補正（トラブルリカバリ）
- 試合リセット、リハーサルモード（早送り速度）
- 回答テキスト入力（任意機能）

### Phase 6: 実戦投入 → DOC統合検討
- 身内リハ → イベント本番
- 本番後、DOCのルーム/認証基盤への統合を評価

## 6. リスク・要検討

- **前進速度のバランス**: 速すぎると考える間がなく、遅すぎると緊張感がない。リハ必須。「残り距離が減るほど減速」等の緩和カーブも選択肢
- **会場回線**: 観客数十人が同時接続する場合のWi-Fi/モバイル回線品質。WebSocket切断時のポーリングフォールバックを検討
- **投票の偏り**: 知り合い票など。イベント用途なので許容だが、得票数の生表示か割合表示かは演出判断
