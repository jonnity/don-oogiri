# GOAL: ドン大喜利 Phase 1 — コアエンジン

このドキュメントはClaude Code（または他の自律コーディングエージェント）に渡し、実装を進めさせるためのゴール定義。詳細なルール仕様は同梱の `heikindai-oogiri-spec.md` を必ず先に読むこと。

## これは何か

生の大喜利イベントで使う「平均台（どんじゃんけん）形式」の対戦進行ツール。3人vs3人のチームが、交互に回答を出し合い、観客投票で勝敗を決めながらマーカー（陣取り）を進めていく。マーカーが相手陣地の端に到達したチームの勝ち。

詳細ルールは `heikindai-oogiri-spec.md` の「1. 確定事項」を正としてそのまま実装すること。要約や独自解釈で仕様を変えない。

## Phase 1のゴール（Definition of Done）

以下が満たされたら Phase 1 完了とする：

1. Cloudflare Workers + Durable Object で1試合＝1ルームとして状態を管理できる
2. `heikindai-oogiri-spec.md` の状態遷移（`initial_writing → voting → challenge_writing → voting → ... → finished`）が実装され、以下がすべて正しく動く：
   - INITIAL_WRITINGで両チームが同時に「執筆完了」を通知でき、先着した方が自動前進を開始する
   - CHALLENGE_WRITING中、マーカーはサーバ側のタイマーで自動的に前進し続ける（誰も止められない）
   - 「指名」アクションで前進が停止し、位置がその時点で確定する
   - VOTEの結果に応じて正しい遷移が起きる：進んでる側が勝てばそのままCHALLENGE_WRITING継続（負けた阻止側の次走者が執筆）、阻止する側が勝てばその位置から反転し、この回答が新たな進んでる側になる（旧・進んでる側の次走者が新たな阻止する側になる）
   - マーカーが端に到達した瞬間（前進中でも）に即座に`finished`になる
3. マーカー位置は「前進開始時刻・速度・開始位置・方向」のみをサーバから配信し、クライアント側で現在時刻から補間計算する方式（サーバ権威）。DO側は到達判定用にalarmを使う
4. MC操作用の最低限のUI（**この段階では素のHTML/React、装飾不要のデバッグ用Web UI**。ネイティブアプリ化はPhase 3で別途行うのでここでは扱わない）で、以下を人力実行しながら1試合を最初から最後まで通しで回せる：
   - チーム名・6人のメンバー名を登録して試合開始
   - 各フェーズで「執筆完了」「指名」「投票結果入力（今回は観客投票UIがまだないのでMCが手動で結果を入力してよい）」を操作
   - 現在の状態（フェーズ、進んでる側、マーカー位置、次に執筆すべき人）が画面に表示される
5. 上記フローが自動テスト（DOのロジック部分をユニットテスト、可能ならVitest）でカバーされている。特に「反転で進んでる側が入れ替わる」「前進中の端到達で即finished」の2つは必ずテストする

## 明示的にスコープ外（Phase 1ではやらない）

- 観客のスマホ投票UI・QR joinなど（Phase 2）
- Tauriによるネイティブデスクトップアプリ化（Phase 3）
- マーカーや平均台の見た目のアニメーション・演出（Phase 4。Phase 1はテキストやシンプルなバーで状態が分かれば十分）
- 回答テキストそのものの入力・保存（MCは「どちらが勝ったか」だけ入力できればよい。Phase 5で任意機能として追加）
- 認証、DBへの永続化（D1）、試合履歴。すべてDOのメモリ/storageで完結してよい
- DOCとの統合作業

## 技術方針

- スタック: Hono + Cloudflare Workers + Durable Objects + Vite + React + TypeScript
- 大喜利DOC（既存プロジェクト）と設計思想・ディレクトリ構成を揃える。ただし本ツールは新規リポジトリ/新規プロジェクトとして単体で成立させる（DOCへの依存を作らない）
- WebSocket Hibernation APIの利用を検討（DOC実装時の知見を流用可能）
- パラメータ（前進速度、レーン全長）は将来調整しやすいよう、コード上の定数ではなく試合作成時に指定できる設定値として持たせる
- **将来Tauriでラップすることを見越し**、MC操作卓・投影画面のUIはブラウザ依存のAPI（localStorage/sessionStorage等）に固く依存させず、Vite+Reactの素直な構成にしておくこと。バックエンドとの通信はWebSocketクライアントとして疎結合に保つ

## 進め方への指示

- まず状態機械の型定義（フェーズ、イベント、状態）と遷移ロジックを純粋関数として書き、DOの外に切り出してユニットテストを先に通してから、DOやAPI・UIに配線すること
- 基本的には妥当な解釈で判断して実装を進めてよい。ただし仕様同士が矛盾している、あるいは仕様に明確な穴があってどちらに転んでもゲーム性が大きく変わってしまうような不明点に限り、一旦立ち止まって報告すること
- 実装が一区切りついたら、`heikindai-oogiri-spec.md` の「6. リスク・要検討」に該当する懸念点（前進速度のバランスなど）があれば都度メモに残すこと
