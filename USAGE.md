# 使い方ガイド（インストール〜試合進行）

`DEPLOY.md`がインフラを立てる側の手順なのに対し、こちらは実際にMCとしてイベントを回す側の手順。デプロイ済みの本番URL（`DEPLOY.md`の「現在の本番URL」参照）を前提に書く。

## 誰が何をするか

| 役割 | 使うもの | インストール要否 |
|---|---|---|
| MC | MC操作卓（このアプリ） | 要（下記3パターンから選ぶ） |
| プロジェクター/別モニタ | 投影画面（MC操作卓から別ウィンドウで開く） | 不要（MC操作卓に内蔵） |
| 観客 | 観客投票ページ（スマホのブラウザ） | 不要。QR読み取りでそのままアクセス |

## 1. MC操作卓のインストール

以下3パターンのいずれか。迷ったらパターンA（ブラウザ版）が一番早い。

### パターンA: ブラウザ版（インストール不要・最速）

会場PCのブラウザで観客投票ページと同じ感覚で開くだけ。動作確認や小規模な回にはこれで十分。

- Tauriビルド済みインストーラがある場合はパターンBを使う
- ない場合、開発機から`pnpm --filter @don-oogiri/mc-ui run build && pnpm --filter @don-oogiri/mc-ui run preview`でローカルサーバを立て、会場PCのブラウザでそこにアクセスする

### パターンB: Tauriデスクトップアプリ（推奨・本番向け）

`.github/workflows/tauri-build.yml`をGitHub Actionsで実行して生成したインストーラ（Windowsなら`.msi`、Macなら`.dmg`）を会場PCに配布してインストールする。詳細は`DEPLOY.md`の「3. MC操作卓 / オプションC」を参照。

インストール後、アイコンをダブルクリックすればネイティブウィンドウとして起動する。

### パターンC: 開発機で直接ビルド

会場PCと同じOS上で`pnpm install && pnpm --filter @don-oogiri/mc-ui exec tauri build`を実行し、`packages/mc-ui/src-tauri/target/release/bundle/`配下の成果物をそのPCにコピー/インストールする。詳細は`DEPLOY.md`の「3. MC操作卓 / オプションB」参照。

## 2. 初回起動時の接続設定

アプリ起動直後（またはブラウザ版で初回アクセス時）に「接続設定」画面が出る。以下2つのURLを入力して「保存」:

- **サーバURL**: 例 `https://don-oogiri-server.jonny1996-ty.workers.dev`
- **観客投票ページURL**: 例 `https://don-oogiri-audience.pages.dev`

保存するとブラウザ版は`localStorage`に、以後の起動では毎回入力し直さなくてよい（画面上部の「接続設定を変更」からいつでも編集可能）。

## 3. 試合の作成

接続設定が終わると「試合作成」フォームが出る。入力項目:

- **お題**: 大喜利のお題本文
- **参加者一覧（事前登録・任意）**: 出演者の名前を先に登録しておける。登録した名前はメンバー名欄の入力補完（datalist）に出てくるので選ぶだけで入力できる（自由入力も引き続き可）。ブラウザのlocalStorageに保存され、試合をまたいで使い回せる
- **チームの人数**: 1人（タイマン）〜3人から選択（既定3人）。選ぶとメンバー名の入力欄がその人数分に変わる
- **🔴赤チーム / 🔵青チーム**: それぞれ選択した人数分のメンバー名
- **端から端までの到達時間(秒)**: マーカーが陣地の端から端まで進む速さの基準（既定120秒）。会場の空気や尺に合わせて調整する
- **試合全体の制限時間**: オンオフ切り替え可能（既定オン・3分）。回答の執筆待ちや投票中は消費されず、マーカーが実際に前進していた時間だけ減っていく。詳しい確定ロジックは次項「制限時間まわりの決着ルール」参照

チームの人数・到達時間・制限時間は、ゲームのコンフィグのように**前回試合作成時に使った値が次回のデフォルト値になる**（お題・メンバー名は毎回入力し直す）。初回のみ上記の既定値が使われる。

「試合を作成」を押すと:
1. 試合が作成され、MC操作卓の画面が試合進行画面に切り替わる
2. **投影画面が自動で別ウィンドウとして開く**（閉じてしまった場合は「投影画面を開く」ボタンで再オープン可能）
3. 「観客投票URL」がMC操作卓上に表示される（コピーボタンあり）。投影画面には同じURLのQRコードが表示される（お題を目立たせたい場面ではOpsPanelの「QRコードを隠す」で一時的に消せる）

## 4. 観客の参加

観客は投影画面のQRコードをスマホのカメラで読み取るだけ（アプリ不要、ブラウザで完結）。1端末につき1票（`localStorage`ベース）。投票は「選択→決定」の2段階で、決定後も「投票を変更する」から締め切られるまで何度でも選び直せる（今どちらを選んでいるかは常にハイライト表示）。得票数は観客側には表示されない（「投票済み」とだけ表示、バンドワゴン効果を避けるため）。MC操作卓側にはリアルタイムの生集計が見える。同数票の場合は「防衛成功」（反転はしない）として扱われるが、同じ顔合わせが続かないよう両チームとも次走者に交代し、その場でゼロから両チーム同時に回答を書き直す（TIE_WRITINGフェーズ、次項参照）。

## 5. 試合進行（フェーズごとのMC操作）

MC操作卓の「状態」パネルに現在のフェーズ・進んでる側・阻止する側・今の回答者（各チームの現在アクティブな回答を書いたメンバー名）・次の回答者（今まさに執筆中のメンバー名、両チーム同時執筆中は2人分）が常に表示される。フェーズごとにMCが押すボタン:

| フェーズ | 状況 | MCの操作 |
|---|---|---|
| 準備中(setup) | 試合作成直後 | 「試合開始」ボタンを押す |
| INITIAL_WRITING | 両チームが最初の回答を執筆中（口頭発表が基本、記録はしない） | 先に書き終えたチーム側の「🔴/🔵 執筆完了（先着）」を押す |
| TIE_WRITING | 観客投票が同数だったため、両チームとも次走者に交代して新しい回答を執筆中 | INITIAL_WRITINGと同じ操作。先に書き終えたチーム側の「🔴/🔵 執筆完了（先着）」を押す |
| （前進中） | マーカーが自動前進中。誰も止めなければ端まで進み続ける（阻止側は口頭発表が基本の回答をこの間に考えている） | 阻止側の回答ができたら「指名（前進ストップ→投票開始）」を押す。前進停止と投票開始はワンクッションなく同時に起きる |
| VOTE | 観客投票を受付中。現在の集計がリアルタイム表示される | 十分票が集まったら「投票を締め切る」。観客投票を使わない回では「票数を手入力して確定」から手動入力も可 |
| （勝敗確定） | 投票結果に応じて自動で継続・反転・同数書き直しのいずれかへ遷移 | 特に操作不要。マーカーが端に到達した瞬間、進行中でも即座に試合終了になる |
| 試合終了(finished) | 勝者が確定 | 🏆勝者表示を確認。続けて次の試合を作る場合は「次の試合を作る」から |

**キーボードショートカット**: 回答者席の方を見ながら操作できるよう、「状態」パネルの主要な操作（試合開始・執筆完了（先着）・指名・投票を締め切る）はすべて矢印キーでも操作できる（マウス操作と併用可）。文字入力欄にフォーカスがある間は無効。今どのキーがどの操作に対応するかは、パネル上部の凡例と各ボタンの脇（例: `←`）に常に表示される。

- どちらか一方を指定する必要がない操作（試合開始・指名・投票を締め切る）: いずれかの矢印キー（←→↑↓）
- 赤/青どちらかを指定する必要がある操作（執筆完了・先着）: 🔴赤＝**左キー**、🔵青＝**右キー**

観客投票を使わない場合の票数手入力や、運用ツール（前進速度・マーカー位置補正・試合リセット等）にはキーボードを割り当てていない（頻度が低い/数値入力を伴うため、マウス・タップ操作のみ）。投票を締め切る操作もキー一発で確定するため、矢印キー付近を触る際は誤操作に注意。

> matchId（試合セッション）はMC操作卓のlocalStorageに保存され、ページ再読み込みやアプリ再起動をしても同じ試合に繋がり直す。QRコードや観客の投票状況を維持したまま続行できる。1イベント内で複数試合をこなす場合は「次の試合を作る」を使うこと。これは**QRコードを維持したまま**チーム・お題だけを入力し直す操作で、観客は再スキャン不要（既に繋がっているQR/セッションのまま次の試合に自動で切り替わる）。イベントを跨ぐ等、意図的にQRコードごと切り替えたい場合だけ「セッションを破棄して新規開始」を使う（観客に再スキャンしてもらう必要がある）。

## 5.5 制限時間まわりの決着ルール

試合全体の制限時間タイマー（画面上部に表示）は、**マーカーが実際に前進(advancing)していた時間だけ**を消費する。回答の執筆待ち（両チームがまだ書き終えていない間）や投票中は、どれだけ時間がかかっても減らない。長尺回答や長い協議で時間を稼がれても不利にならないようにするための仕様。

残り時間を使い切った後の決着はサドンデス方式（スプラトゥーンのガチマッチの延長に近い）:

- 阻止する側には、使い切った後も「指名」が常に許可される（ラスト1答）。回答を書き終えていれば取りこぼされない
- 指名→投票が解決するたびに判定する: その時点でレーン占有量が優勢な側と、現在「進んでる側」が一致していれば（＝逆転の途上でなければ）そこで試合終了。マーカー位置が優勢な方の勝ち
- まだ逆転の途上（劣勢だった側が押し返している最中）なら終了させず、次の指名→投票までサドンデスを継続する
- これを、優勢側が投票で同票以上を取って止める（逆転を阻止する）か、劣勢側が実際に逆転してレーン占有量で上回るまで繰り返す
- マーカーが陣地の端に到達した場合（アリーバル）は、この判定と無関係に常に即座にその場で試合が確定する（投票や制限時間を待たない）

残り時間を使い切ったが試合がまだ続いている間、タイマー表示は「延長中」になる。

## 6. 運用ツール（トラブルリカバリ・リハーサル用）

MC操作卓下部の「運用ツール」を開くと使える機能（`don-oogiri-spec.md` Phase5相当）:

- **お題の訂正**: 誤字修正など軽微な訂正用（ゲーム性に関わる「お題チェンジ」ではない）
- **QRコードの表示/非表示**: 投影画面の主役をお題に譲りたいときに一時的に隠す
- **前進速度**: 本番速度/リハ用5倍速/リハ用10倍速のプリセット切り替え。advancing中に変更しても現在位置を基準に引き継がれるので巻き戻りやジャンプは起きない
- **マーカー位置の手動補正**: advancing/frozen中のみ有効。誤操作などのリカバリ用。端（0または`laneLength`）に補正すると、その場で試合が確定する仕様なので取り扱い注意
- **試合リセット**: チーム名・メンバー・お題・パラメータ・速度倍率・QR表示設定は維持したまま、進行状態だけをsetup直後に戻す。リハーサル後、本番前に使うことを想定

## 7. リハーサル → 本番の推奨フロー

1. 会場のWi-Fi/回線でQR読み取り→観客投票ページ到達までを事前に必ずテストする（`DEPLOY.md`のチェックリスト参照）
2. リハーサルは運用ツールの「リハ用5〜10倍速」でテンポよく通しで確認する
3. リハーサル後、本番前に「試合リセット」でリハーサル分の進行状態をクリアする（チーム名・お題・パラメータは再入力不要）
4. 本番速度に戻し忘れていないか確認してから開始する
