# 使い方ガイド（インストール〜試合進行）

`DEPLOY.md`がインフラを立てる側の手順なのに対し、こちらは実際にMCとしてイベントを回す側の手順。デプロイ済みの本番URL（`DEPLOY.md`の「現在の本番URL」参照）を前提に書く。

## 誰が何をするか

| 役割 | 使うもの | インストール要否 |
|---|---|---|
| MC | MC操作卓（このアプリ） | 要（下記3パターンから選ぶ） |
| プロジェクター/別モニタ | 投影画面（MC操作卓から別ウィンドウで開く） | 不要（MC操作卓に内蔵） |
| 観客 | 観客投票ページ（スマホのブラウザ） | 不要。QR読み取りでそのままアクセス |

## 1. MC操作卓のインストール

以下3パターンのいずれか。迷ったらパターンA（ブラウザ版）が一番早い。

### パターンA: ブラウザ版（インストール不要・最速）

会場PCのブラウザで観客投票ページと同じ感覚で開くだけ。動作確認や小規模な回にはこれで十分。

- Tauriビルド済みインストーラがある場合はパターンBを使う
- ない場合、開発機から`pnpm --filter @don-oogiri/mc-ui run build && pnpm --filter @don-oogiri/mc-ui run preview`でローカルサーバを立て、会場PCのブラウザでそこにアクセスする

### パターンB: Tauriデスクトップアプリ（推奨・本番向け）

`.github/workflows/tauri-build.yml`をGitHub Actionsで実行して生成したインストーラ（Windowsなら`.msi`、Macなら`.dmg`）を会場PCに配布してインストールする。詳細は`DEPLOY.md`の「3. MC操作卓 / オプションC」を参照。

インストール後、アイコンをダブルクリックすればネイティブウィンドウとして起動する。

### パターンC: 開発機で直接ビルド

会場PCと同じOS上で`pnpm install && pnpm --filter @don-oogiri/mc-ui exec tauri build`を実行し、`packages/mc-ui/src-tauri/target/release/bundle/`配下の成果物をそのPCにコピー/インストールする。詳細は`DEPLOY.md`の「3. MC操作卓 / オプションB」参照。

## 2. 初回起動時の接続設定

アプリ起動直後（またはブラウザ版で初回アクセス時）に「接続設定」画面が出る。以下2つのURLを入力して「保存」:

- **サーバURL**: 例 `https://don-oogiri-server.jonny1996-ty.workers.dev`
- **観客投票ページURL**: 例 `https://don-oogiri-audience.pages.dev`

保存するとブラウザ版は`localStorage`に、以後の起動では毎回入力し直さなくてよい（画面上部の「接続設定を変更」からいつでも編集可能）。

## 3. 試合の作成

接続設定が終わると「試合作成」フォームが出る。入力項目:

- **お題**: 大喜利のお題本文
- **🔴赤チーム / 🔵青チーム**: それぞれメンバー名3人分
- **端から端までの到達時間(秒)**: マーカーが中央から陣地端まで進む速さの基準（既定180秒 = 端から端で360秒）。会場の空気や尺に合わせて調整する

「試合を作成」を押すと:
1. 試合が作成され、MC操作卓の画面が試合進行画面に切り替わる
2. **投影画面が自動で別ウィンドウとして開く**（閉じてしまった場合は「投影画面を開く」ボタンで再オープン可能）
3. 「観客投票URL」がMC操作卓上に表示される（コピーボタンあり）。投影画面には同じURLのQRコードが表示される（お題を目立たせたい場面ではOpsPanelの「QRコードを隠す」で一時的に消せる）

## 4. 観客の参加

観客は投影画面のQRコードをスマホのカメラで読み取るだけ（アプリ不要、ブラウザで完結）。1端末につき1票（`localStorage`ベース）。得票数は観客側には表示されない（「投票を受け付けました」とだけ表示、バンドワゴン効果を避けるため）。MC操作卓側にはリアルタイムの生集計が見える。

## 5. 試合進行（フェーズごとのMC操作）

MC操作卓の「状態」パネルに現在のフェーズ・進んでる側・次に執筆すべき人が常に表示される。フェーズごとにMCが押すボタン:

| フェーズ | 状況 | MCの操作 |
|---|---|---|
| 準備中(setup) | 試合作成直後 | 「試合開始」ボタンを押す |
| INITIAL_WRITING | 両チームが最初の回答を執筆中（口頭発表が基本、記録は任意） | 先に書き終えたチーム側の「🔴/🔵 執筆完了（先着）」を押す |
| （前進中） | マーカーが自動前進中。誰も止めなければ端まで進み続ける | 阻止側が答えを思いついたら「指名（前進ストップ）」を押す |
| （執筆中/frozen） | 指名されたチームが回答を執筆中 | 書き終わったら「回答完了 → 投票へ」（回答内容欄は任意入力） |
| VOTE | 観客投票を受付中。現在の集計がリアルタイム表示される | 十分票が集まったら「投票を締め切る」。観客投票を使わない回では「票数を手入力して確定」から手動入力も可 |
| （勝敗確定） | 投票結果に応じて自動で継続 or 反転して次の執筆フェーズへ遷移 | 特に操作不要。マーカーが端に到達した瞬間、進行中でも即座に試合終了になる |
| 試合終了(finished) | 勝者が確定 | 🏆勝者表示を確認。続けて次の試合を作る場合は「新しい試合を作る」から |

## 6. 運用ツール（トラブルリカバリ・リハーサル用）

MC操作卓下部の「運用ツール」を開くと使える機能（`don-oogiri-spec.md` Phase5相当）:

- **お題の訂正**: 誤字修正など軽微な訂正用（ゲーム性に関わる「お題チェンジ」ではない）
- **QRコードの表示/非表示**: 投影画面の主役をお題に譲りたいときに一時的に隠す
- **前進速度**: 本番速度/リハ用5倍速/リハ用10倍速のプリセット切り替え。advancing中に変更しても現在位置を基準に引き継がれるので巻き戻りやジャンプは起きない
- **マーカー位置の手動補正**: advancing/frozen中のみ有効。誤操作などのリカバリ用。端（0または`laneLength`）に補正すると、その場で試合が確定する仕様なので取り扱い注意
- **試合リセット**: チーム名・メンバー・お題・パラメータ・速度倍率・QR表示設定は維持したまま、進行状態だけをsetup直後に戻す。リハーサル後、本番前に使うことを想定

## 7. リハーサル → 本番の推奨フロー

1. 会場のWi-Fi/回線でQR読み取り→観客投票ページ到達までを事前に必ずテストする（`DEPLOY.md`のチェックリスト参照）
2. リハーサルは運用ツールの「リハ用5〜10倍速」でテンポよく通しで確認する
3. リハーサル後、本番前に「試合リセット」でリハーサル分の進行状態をクリアする（チーム名・お題・パラメータは再入力不要）
4. 本番速度に戻し忘れていないか確認してから開始する
